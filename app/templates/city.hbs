{{page-title "Weather Forecast"}}
{{@model.city.name}} - {{@model.city.id}}
<br />
{{#each @model.dailyForecasts as |day|}}
  <DayForecast @day={{day}} />
{{/each}}
{{#if @model.error}}
  <p>{{@model.error}}</p>
{{/if}}
